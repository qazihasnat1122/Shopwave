<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied while the process is starting (light mode) -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Theme applied as soon as the process has started.
         Dark window background so there is no white flash before Flutter renders. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">#0A0A0F</item>
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>
